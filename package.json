{
  "dependencies": {
    "express": "^4.18.1",
    "react-helmet": "^6.1.0"
  },
  "name": "workoutpartner",
  "version": "1.0.0",
  "main": "",
  "scripts": {
    "start": "node backend/server.js",
    "install-frontend": "cd frontend && npm install && npm start && cd ..",
    "install-backend": "cd backend && npm install && npm start && cd .."
  },
  "engines": {
    "node": "16.14.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
