{
  "dependencies": {
    "react-helmet": "^6.1.0"
  },
  "name": "workoutpartner",
  "version": "1.0.0",
  "main": "",
  "scripts": {
    "start": "npm-run-all --parallel install-frontend install-backend",
    "install-frontend": "cd frontend && npm install && npm start && cd ..",
    "install-backend": "cd backend && npm install && npm start && cd .."
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
